# ─── HTTP / API Clients ───────────────────────────────────────────────────────
requests>=2.31.0          # General-purpose HTTP client (Philosophers API, Gutenberg)

# ─── Data Manipulation ────────────────────────────────────────────────────────
pandas>=2.0.0             # Robust data analysis and manipulation
# ─── Object Store / Cloud ─────────────────────────────────────────────────────
boto3>=1.34.0             # AWS SDK for Python — used to talk to MinIO (S3-compatible)

# ─── Configuration / Secrets ──────────────────────────────────────────────────
python-dotenv>=1.0.0      # Loads .env files into os.environ

# ─── Orchestration ────────────────────────────────────────────────────────────
apache-airflow>=2.9.0     # Workflow orchestration (DAG scheduling)

# ─── Delta Lake (future-proof for Trusted Zone) ───────────────────────────────
deltalake>=0.17.0         # Python bindings for delta-rs (Delta Lake format support)
pyarrow>=15.0.0           # Underpinning for Delta/Parquet storage

# ─── Streaming / Messaging ────────────────────────────────────────────────────
kafka-python-ng>=2.2.0    # Kafka client (Producer/Consumer)

# ─── Exploitation Zone — vector store + embeddings ────────────────────────────
# Used only by exploitation/structured/corpus_chunks.py: the Spark job that
# chunks the Gutenberg/Wikipedia/Wikiquote/SE corpus and writes embeddings to
# Milvus for the chatbot's RAG retrieval. Justified by data volume (~161 books,
# tens of thousands of chunks) + a real downstream consumer (Reasoner/Voice).
pymilvus>=2.4.0,<2.5.0    # Milvus client (must match the v2.4.x server image)
sentence-transformers>=3.0.0  # all-MiniLM-L6-v2 sentence embeddings (384-dim)

# ─── Consumption Zone — podcast generation ────────────────────────────────────
# Consumed only by consumption/llm.py: the Anthropic-backed llm_fn the
# Interviewer/Reasoner/Voice agents call to generate in-voice podcast turns.
anthropic>=0.40.0

